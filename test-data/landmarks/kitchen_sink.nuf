---
scope: page
---

/*
Automatically adds skip link on top of the page that skips to this element

Compiles to:
```html
<main id="nuf--maincontent"></main>
```
*/
@(main, {
	canSkipTo: true
})

/*
Automatic heading level calculation

Compiles to:
```html
<h1>Content categories</h1>
```
*/
#("Content categories")

/*
Automatic heading level calculation and association with a content section

Compiles to:
```html
<section aria-labelledby="nuf--content-categories-title">
	<h2 id="nuf--content-categories-title">Content categories</h2>
</section>
```
*/
#("Content categories") -> @(section)

/*
Regular text

Compiles to:
```html
<p>Most HTML elements are a member of one or more content categories — these categories group elements that share common characteristics</p>
```
*/
"Most HTML elements are a member of one or more content categories — these categories group elements that share common characteristics"

/*
Assign text to button

Compiles to:
```html
<button>Log in</button>
```
*/
"Log in" -> interaction(action)

/*
Assign text to link

Compiles to:
```html
<a href="/login">Log in</a>
```
*/
"Log in" -> interaction(navigation, {
	href: "/login"
})

